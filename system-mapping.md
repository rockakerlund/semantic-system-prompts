# System Mapping Prompt

A way to get AI (or a team) to reveal the hidden structure behind a problem.

**Prompt:**
"Map this issue as a system: list the parts, their relationships, and the flows between them."

**Why this works:**
- Transforms complex confusion into a visible map
- Makes patterns easier to see and act on
- Builds systemic awareness instead of isolated fixes
